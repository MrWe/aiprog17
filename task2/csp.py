class CSP:
    def __init__(self, rows, columns):
        self.rows = rows
        self.columns = columns
        self.board = [[0 for x in range(len(columns))] for y in range(len(rows))]

    def revise(self):
        pass


    def is_valid(self, board):
        pass









